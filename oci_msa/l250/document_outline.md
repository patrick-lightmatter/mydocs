Please generate a comprehensive Architecture Specification Document for our 106.25 Gbps NRZ optical link based on the codebase you have access to. The document must follow the exact structure below. Whenever possible, extract the actual parameter names, variable bit-widths, and logic structures directly from the code.General Requirements Throughout the Document:Tables: Use markdown tables heavily for parameters, variables, and truth tables.Code Snippets: Insert Python code snippets where appropriate to explain the logic (especially in the CDR and Adaptation sections).Dead Bands / Hysteresis: Explicitly mention where dead bands or hysteresis bands are used in algorithms and explain how they are implemented.Section 1: Link OverviewCreate a top-level block diagram of the link.Outline the primary goals of the system.Detail the target performance metrics, including the target BER and energy efficiency (pJ/b).Describe the OCI-MSA standard alignment and state that we are operating at 106G NRZ.Section 2: Basic Background & TerminologyExplain the basic terminology of the system.Error Slicers vs. Data Slicers: Define what an error slicer is and what a data slicer is. Explain how each error slicer has a dedicated DAC providing its reference voltage, and that a control loop manages this voltage. Explain the impact of DC offset on the data slicer.Channel Response: Define the $h_{-1}$ (pre-cursor), $h_0$ (main cursor), and $h_1$ (post-cursor) taps.Visuals: Insert a placeholder or ASCII/Markdown figure showing a sample impulse response illustrating these cursors.Section 3: High-Speed Driver SpecificationProvide the high-level specifications and block parameters for the TX high-speed driver based on the design.Section 4: TIA SpecificationProvide the high-level specifications and parameters for the Receiver Transimpedance Amplifier (TIA).Section 5: Clock and Data Recovery (CDR)Detail the entire fixed-point implementation architecture of the CDR.Parameter Tables: List every accumulator, multiplier, and control parameter as variables (e.g., accumulator bit width = N_accum) in a clean table format.Downsampling: Specifically explain the voting mechanism used to downsample the high-speed data.Data Paths: Describe the phase and frequency paths.Frequency Accumulator: Explain how to set the frequency accumulator width for a specific ppm offset, and detail its saturation logic.Truth Tables: Include the phase detector/CDR truth tables mapping input states to phase updates.Section 6: Adaptation LoopsDetail the digital adaptation loops (Offset, $h_0$, $h_1$, $h_{-1}$).Common Architecture: Start with a subsection outlining the shared digital architecture across all loops (e.g., vote $\rightarrow$ scale votecount $\rightarrow$ accumulate $\rightarrow$ divide/chop $\rightarrow$ produce DAC code).Specific Loops: Create subsequent subsections for each specific loop. Describe the individual algorithm and how it maps to the shared architecture.Parameter Tables: Include tables with the fixed-point implementation placeholder variables for each loop's accumulators and gains.Truth Tables: Include the truth tables dictating the update logic (up/down/hold) for each adaptation loop.